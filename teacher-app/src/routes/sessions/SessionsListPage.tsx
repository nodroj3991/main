import { Placeholder } from "../Placeholder";
export function SessionsListPage(): React.ReactElement {
  return (
    <Placeholder
      title="Sessions / Lesson Plans"
      message="Sessions auto-generate from each Scheme of Work row. The full TMC editor (Connect / Share / Apply / Recall&Review / Stretch&Challenge / Conclusion + embedded skills + character strengths) is being wired up next."
    />
  );
}
