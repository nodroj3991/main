import { Placeholder } from "../Placeholder";
export function SessionEditorPage(): React.ReactElement {
  return (
    <Placeholder
      title="Session editor (TMC)"
      message="Full TMC lesson-plan editor with [C] [R] [T] [L] [?] markers, embedded fields and PPT round-trip is being implemented next."
    />
  );
}
