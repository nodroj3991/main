import { Placeholder } from "../Placeholder";
export function DecksPage(): React.ReactElement {
  return (
    <Placeholder
      title="PPT Decks"
      message="Generate decks from Schemes of Work and round-trip edits back into lesson plans. Coming next."
    />
  );
}
