import { Placeholder } from "../Placeholder";
export function ModuleSpecsPage(): React.ReactElement {
  return (
    <Placeholder
      title="Module Specifications"
      message="Load and edit module specifications. Coming next: import a Word/PDF spec, save sections as reusable snippets, link to syllabus points."
    />
  );
}
