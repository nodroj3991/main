import { Placeholder } from "../Placeholder";
export function GradebookPage(): React.ReactElement {
  return (
    <Placeholder
      title="Gradebook"
      message="Per-group, per-module grade tracker with rubrics and feedback. Coming next."
    />
  );
}
