import { Placeholder } from "../Placeholder";
export function AssessmentsPage(): React.ReactElement {
  return (
    <Placeholder
      title="Assessments & Quizzes"
      message="Collate questions tagged during sessions into formative quizzes per module, with marking keys and exportable formats. Coming next."
    />
  );
}
